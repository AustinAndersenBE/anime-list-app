datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum AnimeStatus {
  WATCHED
  NOT_WATCHED // get rid of the this later
  WILL_WATCH
}

model User {
  id            Int @id @default(autoincrement())
  username      String @unique
  email         String @unique
  passwordHash  String
  isAdmin       Boolean @default(false)
  userAnimeList UserAnimeList[] // one-to-many
  posts         Post[]
}

model UserAnimeList {
  id          Int @id @default(autoincrement())
  userId      Int
  externalAnimeId  String
  animeTitle  String
  status      AnimeStatus
  rating      Float?
  user        User  @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, externalAnimeId], name: "userId_externalAnimeId")
}


model Post {
  id            Int @id @default(autoincrement())
  userId        Int
  externalAnimeId    String?
  episodeNumber Int? // deprecate this for now 
  title         String
  content       String
  createdDate      DateTime @default(now()) // enforce this at application level
  updatedDate    DateTime @updatedAt
  user          User @relation(fields: [userId], references: [id], onDelete: Cascade)

}